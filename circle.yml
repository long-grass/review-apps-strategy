deployment:
  master:
    branch: master
    commands:
       - git subtree push --prefix client git@heroku.com:pacific-ridge-92870.git master

  develop:
    branch: develop
    commands:
       - git subtree push --prefix client git@heroku.com:dev-pipeline.git master

test:
  override:
    - ./test.sh